[pytest]
addopts = --maxfail=20 -m "not premium"
markers =
    unit: Unit tests (no outside dependencies)
    integration: Integration tests against live API
    premium: Requires premium workspace (skipped)
testpaths = toggl-cli/tests/integration
